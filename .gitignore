# IDEA settings
.idea/
.vscode/
.claude/
warm-nest

# OSX dir files
.DS_Store

# Log
*.log

# 运行时 / 构建产物
/runtime/
/bin/
/dist/
*.tar.gz

# 部署含密文件（DB 密码 / JWT key 等敏感值走主机环境变量注入，不落文件），勿提交
/deploy/deploy.conf
# 生产配置（含真实业务密钥），勿提交
app_prod.toml
# 主机环境变量文件（若在本仓库生成）
*.env
